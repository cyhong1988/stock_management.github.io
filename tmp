在定義完環境屬性後，接下來是 .lib 檔案中最重要的數據結構：查表範本 (lu_table_template)。
由於元件的延遲（Delay）和功耗（Power）並非線性變化的，我們無法用一個簡單的公式概括，因此必須透過「查表」來取得精確數值。
這個範本（Template）的作用主要有三個：
1. 定義自變數 (Variables)：
通常我們會選取兩個關鍵變數，也就是圖中看到的 variable_1：input_net_transition（輸入信號斜率）和 variable_2：total_output_net_capacitance（輸出負載電容）。
2. 設定座標軸 (Index Lists)：
範本會預先定義好一組索引清單，例如圖中範例 comb_0 是一個 3 \times 3 的表格，它規定了當 Slew 是 0.01、0.1 或 0.3，以及 Load 為 0.001 到 0.05 時的座標點。
3. 資源共享與精簡化：
這是最關鍵的一點。一個範本可以被成千上萬個 Cell 同時引用。這不僅大幅減少了 .lib 檔案的體積，也讓維護變得更簡單——當我們需要調整整體的查表精度時，只需修改範本，而不需要去更動每個元件。
小結一下：
範本定義了「骨架」，而具體的 Cell 則填入對應的「數值」。透過這種方式，EDA 工具就能在這些座標點之間進行插值運算（Interpolation），推算出最接近真實物理情況的時序結果。
